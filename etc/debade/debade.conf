{
    ## 作为连接rabbitMQ Server的客户端的配置
    "agent": {
    ## rabbitMQ的地址
        "server": "rabbitmq.debade",
        "port": 5672,
        "user": "guest",
        "password": "guest"
    },
    ## 作为接受事件回调注册的server的配置
    "server": {
    ## server启动时监听的本地端口
        "port": 80
    }
}
